function QuickPlayer({closePopup}) {
  return (
    <div>
      <button className="btn btn-type-3 btn-color-4 close-btn" onClick={closePopup}>Cancel</button>
      Player
    </div>
  )
}

export default QuickPlayer