import React, {useEffect, useState} from 'react'
import axios from 'axios'

import './Campaigns.css'

function Campaigns() {
  // const [campaigns, setCampaigns] = useState([])

  // useEffect(() => {
  //   axios.get('/api/campaign')
  //     .then(res => {
  //       setCampaigns(res.data)
  //     })
  // }, [])

  // const displayCampaigns = campaigns.map((campaign, i) => {
  //   return (
  //     <div className='campaign' key={i}>{campaign.title}</div>
  //   )
  // })

  // return (
  //   <div id='my-campaigns'>
  //     {displayCampaigns}
  //   </div>
  // )
  return (
    <div>
      <p>How did you get here? This page is in development so please leave(meant in the nicest way possible)</p>
    </div>
  )
}

export default Campaigns